# Expo inlines EXPO_PUBLIC_* vars at bundle time. Point the app at the
# Django backend on your Mac's LAN IP so a physical phone can reach it.
# If your Mac's IP changes (new network), update this and restart `expo start -c`.
EXPO_PUBLIC_API_URL=http://192.168.1.77:8000
